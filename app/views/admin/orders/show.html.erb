<%= render "admin/shared/header" %>

<h3 class="my-3 ml-3">注文履歴詳細</h3>

購入者<%= @order.customer.full_name %><br>
注文日<%= l @order.created_at %><br>
配送先<%= @order.address_display %><br>
支払方法<%= @order.payment_method_i18n %><br>
注文ステータス<br>


<div class="col-5 ml-3">
  <table class="table">
    <thead>
      <tr>
        <th>商品名</th>
        <th>単価(税込)</th>
        <th>数量</th>
        <th>小計</th>
        <th>製作ステータス</th>
      </tr>
    </thead>

    <tbody>
      <% @order_details.each do |order_detail| %>
      <tr>
        <td><%= order_detail.item.name %></td>
        <td><%= order_detail.item.add_tax_price.to_s(:delimited) %></td>
        <td><%= order_detail.amount %></td>
        <td><%= order_detail.subtotal.to_s(:delimited) %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>

商品合計<%= (@order.total_payment - @order.shipping_cost).to_s(:delimited) %>円<br>
送料<% @order.shipping_cost = 800 %><%= @order.shipping_cost %>円<br>
請求金額<%= @order.total_payment.to_s(:delimited) %>円<br>